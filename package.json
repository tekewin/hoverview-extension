{
  "name": "hoverview-extension",
  "version": "1.2.2",
  "private": true,
  "description": "HoverView Chrome Extension – test suite",
  "scripts": {
    "test": "jest --runInBand",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e",
    "icons": "node tools/render-icons.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
